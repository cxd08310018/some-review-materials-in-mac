'''
如果想在python中定义常亮的的话  变量的名称必须是大写才能变成常量 例如  
但是 这个常量是可以修改的 意思是 如果在代码中看到有大写的字母常量不要擅自的去修改它 
是可以修改 但是不要修改
'''
NAME = '赵四'
AGE = 23

使用type()是可以查看数据类型。填入括号里面

### 注释：
    python中的注释为 #  ''' ''' """ """ 还有就是在python文件的开头如果使用三个双引号进行注释的话算是文档字符串  如果想写文档编码注释的话也要写到文档开头  先要写文档编码注释  在进行下面的写入  文档编码注释为：#coding=utf-8  如果修改错误的话把utf-8修改成iso8859-1的话 在正常的PyCharm里面可能会有明显的提示但是在vscode里面 本人没有看到提示。或者是修改一个右下角的UTF-8进行尝试。

### 数据类型
    python中变量是没有类型的  数据是有类型的  

### 整形
    python中的整形  当你输入的数字非常大的时候使用下划线进行分组,三个为一组 例如  a = 12_000_000 打印的时候直接输出12000000  不会报错。
    python中的整数的上限值，取决于执行代码的计算机的内存和处理能力。如何查看  例如  a = 9 ** 9999  意思是9的9999次方 在进行输入一下，看一下电脑是否能成功的输出之类的。如果报错并且提示4300的话意思是当你执行print（a）打印这个a的时候这个print函数会将次数字自动转换为字符串 然后在进行打印  只不过这个报错提示是这个字符串的有效长度为4300 不能超过  如果想证明的话  证明方式为 a = 9 ** 9999  b = a + 100  在进行执行  看是否报错，如果没有报错说明能执行，要想修改字符串的有效长度的话 使用函数 sys.set_int_max_str_digits(0) 这句话的意思是解除字符串的限制。还需要引入import sys 

### 浮点型
    科学计数法：
    # 浮点型的科学计数法表示。
        speed_of_sound = 3.4e+2# 3.4乘以10的2次方。

        world_population = 7.8e9# 7.8乘以10的9次方。

        distance_sun_earth = 1.496E8# 1.496乘以10的8次方。

        speed_of_light = 2.998E+8# 2.998乘以10的8次方。

        one_ml = 1e-3#1乘以10的-3次方。

        one_mg = 1E-3# 1乘以10的-3次方。

### 字符串
    message = ('') 可以这么写。  批量注释 也是字符串 例如 """ """ ''' ''' 都是字符串的表达式  只不过如果直接使用的话 正常可能编译但是没有变量接受无法输出的。
### 字符串格式化输出
    使用占位符的方式进行字符串拼接的效果 例如info = '我叫%s' % name 前提是有这个name这个变量 如果有两个需要填写的话 使用的方式为：info = '我叫%s,我是%s' % (name,name1) 如果后面输入的是小数的话，占位符为%f 整数的话，占位符为%i  例如info = '我叫%s,我是%s,体重%f,年龄%i' % (name,name1,g,age)前提是你要有这些变量。也可以把i修改成d  表示十进制的整数。也可以修改成 info = '我叫%s,我是%s,体重%s,年龄%s'% (name,name1,g,age) 也可以自动转换完成之后展示！！。如果把小数部分修改成%i  python自动转换为整数  小数点后面的数字自动丢弃了。%s是万能的。

    也可以使用f-string  使用方式为info = f'我是{name},我叫{name1}......'  前提需要这些变量已经定义的。是python中最推荐的一种方式。
### 字符串占位符精度控制
    如果只验证n的话 代码使用方式为  info = '%.2s' % name  这个s和其他的占位符是一样的。
    %m.ns 
    其中m作用：
        1.字符串的最小宽度，位数不够的话用空格会补全，位数小于字符串长度的话不起作用
        2.正数是右对齐，负数是左对齐
    其中n作用：
        精度控制，最多输出n个字符串（若n大于实际的字符串长度，则不起作用）。
    代码展示：info = '%3.2s' % name 
    
    %m.nf
    其中n作用：
        精度控制，最多输出n个小数（默认值为6），不够用0来补，截断的时候会四舍五入
    其中m作用：
        和上面的字符串的m一样的
    
    %m.nd
    其中m作用
        和上面的字符串的m一样的
    其中的n作用
        精度控制，最少用n位显示数字，位数不够用0来补，位数小于正数，则自动失效。

### 字符串中的转义字符
    用法：
        如果在print中使用的方式为\' 意思是要输出一个'这个单引号 双引号和前面的单引号用法是一样的。\" 输出一个"双引号
        使用 \n 意思是换行
        使用\\两个反斜杠 会输出一个\反斜杠
        使用\b 是删除这个\b的前一个字符  能实现打字机的效果
        使用\r 是光标回到本行开头，覆盖输出  能实现进度条的到达百分之几的效果
        使用\t表示水平制表符（让光标跳转到下一个制表位）但是如果在输出的时候使用的expandtabs(4)的话 意思是我不管你在什么环境下执行这条输出语句  它的制表位永远是4位
        print(ab\tcd.expandtabs(4))

### 数据类型转换
    用户输入的内容都是字符串，如果需要进行数学运算的话，就必须进行数据转换
    对文件进行写入的时候，要将其他类型的数据转换为字符串
    从数据库中读取出来的内容都是字符串，如果需要进行数学运算的话，也需要进行数据类型的转换
    
    把指定的数据转换为字符串：str(数据) 也可以使用变量去接受这个值 例如  a = str(18) .....  等等。任何类型都可以转换字符串类型 科学计数法也可以使用。

    把指定的数据转换为整形：int(数据) 如果是小数的话，直接把小数点后面的全部删除掉了，只输出正数。如果int(' 7 9 ')这样的或者是int('你好')这样的都是不行的，没有数字。不能接受 int('28你') 也不能接受int('14.5') 

    把指定的数据转换为浮点型:float(数据) 不能接受的是 float(' 5. 7 ') 或者是float('你好') 或者是float('5.7你')或者是float('5.23.17')

### 算数运算符
    加减乘除  除 最终的结果给的是浮点数  //取整数 9//6对个这个计算出来的结果返回是正数不执行四舍五入。 %是取余数   ** 指数运算  2 ** 3 意思是2的三次方。
### 符合运算符
    gae+=1  这个符合运算符 可以使用与加减乘除都可以使用  上面的 // 和 % 和 ** 都可以使用  
### 比较运算符
    自己去练习或者是去学习
### 布尔类型
    true false  使用bool把指定内容转换成布尔类型  书写方法  print(bool(true)) true = 1 false = 0  python中除0以外的任何数，转为布尔值后都为true  如果里面写的是一个负值 print(bool(-10))也是turn python只关心数字是否为0 其它的不关心。python中除了空字符串以外的任何字符串，转为布尔值都是true   
### 逻辑运算符
    与 或 非  自己去学习

### 进制  自己去学  这边不多余过多解释 下面只解释进制转换的函数
    无！！！！！！ 
    bin() 十进制转换为二进制字符串
    oct() 十进制转换为八进制
    hex() 十进制转换为十六进制
    int() 其他类型转换为十进制数字

### 输入语句
    print('请输入：')
    age = input() // 不管你输入是什么类型  但是输出的全是字符串类型。
    print(f'你输入的语句是{age}')  //age = int(age) print(f'你输入的语句是{age + 1}') 都可以正常运行的
    input('请输入：')

### 流程控制语句
    略过！

中间的单分支或者是多分支之类的  自己去学  此处略过！！！
多分支练习：
    1 参赛年龄必须是：18~45岁之间。
    2 参赛前必须提交体检报告，否则不能比赛。
    3 比赛结束后，根据会员的等级，领取不同礼物：
        1级会员礼物为：纪念T恤“
        2级会员礼物为：专业跑鞋
        3级会员礼物为：运动耳机

### 循环 
    文字加密和解密的效果
    # 加密文字代码：
    ```
    text = input('请输入要加密的文字：')
    se  = ''
    for t in text:
        se += chr(ord(t) + 1)
    print(f'加密后的文字为：{se}')

    # 解密文字代码：
    se = input('请输入要加密的文字：')
    text  = ''
    for s in se:
        text += chr(ord(s) - 1)
    print(f'加密后的文字为：{text}')
    ```

### while和for循环的区别：
[alt text](image/image.png)

### countinue 和break 区别 
    略过！！！！！

### 函数

位置参数的使用
def fun(num,num1):
    print(f'您点的是：{num}份{num1}')
fun(1,'aaa')

函数关键字参数
    def fun(num,num1):
        print(f'测试{num},{num1}')
fun(num='',num1='')

位置参数和关键字参数区别在于后者是不区分位置的！！例如 fun(num1='',num='') 都可以执行  但是前者不行！！！！  也可以使用位置参数和位置参数混着使用但是前提条件是 位置参数必须在关键字参数的前面。

限制传参方式  /和*的区别在与 这个/前面只能使用位置参数，，*后面只能用关键字参数 例如：def fun(name,/,age,*,name1,name2)  fun('张三',23,name1='',name2='')  前提是/必须写在*的前面   也可以设置默认操作  但是默认参数必须写在必选参数的后面 例如 def fun(name,name='',(之后面写的形参都要写上默认值))
python给print()函数设置的默认值不使用是出不来的。

函数中的可变参数  定义参数的时候，在形参名称前面加上*，可以接受任何数量的位置参数，并打包成一个元组；def fun(*args) 这个里面的args只是一个形参名称，可以随意修改  args中存放的：函数调用时候，所传入的所有位置的实参  args的数据类型是元组  例如  def fun(*args) fun('a','a','b') args = a,a,b  元组
定义函数的时候，在形参名前面加上**kwargs 可以接受任意数量的关键字参数，并打包成一个字典  和上面的args的使用方式一样的 区别在于 args接受的是value，而kwargs接收的key和value，args和kwargs是可以同时使用的，但是必须先写args   args和kwargs也能和其他类型的参数一起使用 
    例如：
    def fun(a,b,*args,c='name',**kwargs) :
        print(a)
        print(b)
        print(args)
        print(c)
        print(kwargs)
    fun('a','b','抽烟','喝酒',c='测试',age=18,he=20)

特殊的字面量 none  意思是现在不知道某一个变量它的具体类型是什么 就可以使用次类型 例如  name = none  如果使用none转换为布尔值的话，初始值是false none不能参与数学运算 同时也不能与字符串拼接  如果不给函数设置返回值，函数默认是none。  


函数返回值：自己去学习谢谢 提示print函数是没有返回值的。

函数中的全局作用域和局部作用域  自己去学习谢谢！！





